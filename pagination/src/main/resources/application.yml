spring:
  jpa:
    showSql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/pagination_db
    username: postgres
    password: Eziomanish@1
